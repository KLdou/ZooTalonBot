# Система валидации и редактирования данных

## Обзор изменений

Добавлена система валидации входящих данных с возможностью интерактивного редактирования через Telegram бот.

## Новый функционал

### 1. Автоматическая валидация данных
- **ФИО**: проверка на наличие минимум 2 слов (Фамилия Имя)
- **Дата**: проверка корректности формата даты
- **Обязательные поля**: проверка на пустоту (телефон, адрес, клиника, тип животного, имя животного, цель визита)

### 2. Интерактивное редактирование
- Показывается **только при обнаружении ошибок**
- Inline кнопки для исправления каждого проблемного поля
- Визуальная индикация: ✅ корректное поле, ❌ поле с ошибкой
- Подробное описание ошибки под каждым проблемным полем

### 3. Кеширование справочников
- Глобальный кеш справочников (клиники, типы лечения) на 24 часа
- При редактировании клиники/типа лечения - валидация через закешированные данные
- **0 дополнительных API запросов** при редактировании

## Архитектура

### Новые файлы

#### `src/utils/validators.js`
Модуль валидации с функциями:
- `validateFIO(fio)` - валидация ФИО
- `validateDate(dateString)` - валидация даты
- `validateAllFields(baseData)` - общая валидация всех полей

### Изменённые файлы

#### `src/services/api.js`
- Добавлен глобальный кеш `refListCache` с TTL 24 часа
- Функция `getRefListCached(endpoint, token, cacheKey)` - получение справочника с кешированием
- Функция `clearRefListCache()` - ручная очистка кеша

#### `src/utils/helpers.js`
- Хранилище сессий редактирования `editSessionStore`
- `storeEditSession()` - сохранение сессии с данными и справочниками
- `getEditSession()` - получение сессии
- `getActiveEditSession()` - получение активной сессии для чата
- `updateEditSession()` - обновление сессии
- `removeEditSession()` - удаление сессии

#### `src/bot/handlers.js`
Добавлены функции:
- `showDataEditInterface()` - показ интерфейса редактирования с кнопками
- `startFieldEdit()` - начало редактирования поля
- `handleFieldEdit()` - обработка нового значения с повторной валидацией
- `validateSpecialFields()` - валидация clinic/type через закешированные справочники

Модифицированы функции:
- `handleMessage()` - добавлена проверка на режим редактирования + валидация
- `handleCallback()` - добавлена обработка `action: "edit_field"`
- `createTalonFlow()` - использование `getRefListCached()` вместо `getRefList()`

## Как это работает

### Сценарий 1: Все данные корректны
```
1. Пользователь отправляет сообщение
2. LLM парсит данные
3. Валидация проходит успешно
4. Обычный flow (создание документа/талона)
```

### Сценарий 2: Обнаружены ошибки
```
1. Пользователь отправляет сообщение с неполными данными
2. LLM парсит данные
3. Валидация обнаруживает ошибки (например, ФИО = "Иванов")
4. Показывается интерфейс редактирования:
   
   ⚠️ Обнаружены проблемы с данными:
   
   ❌ 👤 ФИО: Иванов
      └─ ФИО должно содержать минимум Фамилию и Имя
   ✅ 📞 Телефон: +375291234567
   ✅ 📍 Адрес: г. Минск, ул. Ленина 1
   ...
   
   [✏️ Исправить ФИО]
   [❌ Отменить]

5. Пользователь нажимает "Исправить ФИО"
6. Бот: "✏️ Введите ФИО в формате: Фамилия Имя Отчество"
7. Пользователь: "Иванов Иван Иванович"
8. Повторная валидация всех полей
9. Если ошибок нет - продолжается обычный flow
```

### Сценарий 3: Редактирование клиники/типа лечения
```
1. Пользователь исправляет клинику: "Друг"
2. Валидация через закешированный список клиник (matchEntity)
3. Если клиника найдена - обновление данных
4. Если не найдена - показ ошибки:
   ❌ Клиника "Другг" не найдена в системе. Проверьте название.
   
   Пожалуйста, введите корректное значение:
5. Пользователь вводит правильное название
6. Цикл повторяется до успешной валидации
```

## Производительность

### До изменений:
- API запросы к справочникам при каждом создании талона
- Нет валидации до попытки создания документа

### После изменений:
- Справочники кешируются на 24 часа (1 запрос на весь бот)
- При редактировании - 0 дополнительных API запросов
- Ранняя валидация предотвращает некорректные запросы к API

## Настройки

### TTL кеша справочников
В файле `src/services/api.js`:
```javascript
const CACHE_TTL = 24 * 60 * 60 * 1000; // 24 часа
```

### Время жизни сессии редактирования
В файле `src/utils/helpers.js`:
```javascript
setTimeout(() => editSessionStore.delete(sessionKey), 30 * 60 * 1000); // 30 минут
```

## Тестирование

### Проверка синтаксиса
```bash
node --check src/utils/validators.js
node --check src/services/api.js
node --check src/utils/helpers.js
node --check src/bot/handlers.js
```

### Запуск бота
```bash
npm start
```

### Тестовые сценарии

1. **Неполное ФИО**:
   ```
   Иванов
   +375291234567
   г. Минск, ул. Ленина 1
   Друг
   Мурка
   Кошка
   Стерилизация
   ```
   Ожидаемо: показ интерфейса редактирования с ошибкой ФИО

2. **Некорректная клиника**:
   - Ввести все данные корректно, кроме клиники (например, "Неизвестная клиника")
   - При редактировании клиники ввести несуществующее название
   - Ожидаемо: сообщение об ошибке с предложением ввести корректное значение

3. **Все данные корректны**:
   - Ввести все данные в правильном формате
   - Ожидаемо: обычный flow без показа интерфейса редактирования

## Обратная совместимость

Все изменения **полностью обратно совместимы**:
- Если валидация проходит успешно - работает как раньше
- Существующие функции не изменены (только расширены)
- Кеширование прозрачно для остальной логики

## Будущие улучшения (опционально)

1. Команда `/refresh_cache` для ручной очистки кеша админами
2. Автодополнение при вводе клиники (показ top-5 вариантов кнопками)
3. Статистика валидационных ошибок для анализа
4. Расширенная валидация адреса (проверка наличия номера дома)
5. Валидация телефона на корректность белорусского формата
